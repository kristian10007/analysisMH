# Preparation
Copy the Exel-files to the according folder.
- "Doktorarbeit_Hilke_8.3.xslx" to folder "Hilke3"
- "Tabelle_mit_Patientennummer_K.xslx" to folder "Michelle3"

# Introducion
There are two options to run the notebooks.

(1) Use doker
(2) Run directly with python.

# Using docker
- Install docker (e.g. via https://docs.docker.com/get-docker/ )
- ...

# Using Python
- Install re needed libraries with:
  pip3 install -r docker/requirements.txt
  
- Start Jupyter with:
  jupyter notebook